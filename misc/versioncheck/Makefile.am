# Makefile.am for building the example program 
AUTOMAKE_OPTIONS = foreign 1.5

# headers to install 
instdir=$(exec_prefix)/bin
inst_PROGRAMS = alugridversion
alugridversion_SOURCES = alugridversion.cc 
